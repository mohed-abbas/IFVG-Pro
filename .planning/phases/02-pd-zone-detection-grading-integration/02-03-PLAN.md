---
phase: 02-pd-zone-detection-grading-integration
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/IFVG_Indicator.pine
autonomous: true
requirements: [PDZ-03, DSH-01, DSH-02]

must_haves:
  truths:
    - "Dashboard displays a `PD Zone:` row that shows `PREMIUM` (red), `DISCOUNT` (green), `EQ` (yellow), or `—` (gray) based on current price-mid vs. range"
    - "Dashboard displays a `Range %:` row showing an integer 0-100 followed by `%`, or `—` when no valid range"
    - "Both rows gate on `g_current_range.is_valid` — when false, both cells render `—` in gray (D-06)"
    - "Range % is calculated from current price-mid: `(price_mid - low) / (high - low) * 100`, rounded to integer"
    - "The rows are added without breaking existing dashboard layout (existing 10 rows preserved; new rows appended below)"
  artifacts:
    - path: "src/IFVG_Indicator.pine"
      provides: "Two new dashboard rows in Section 11 render_dashboard()"
      contains: "PD Zone:"
  key_links:
    - from: "render_dashboard() (Section 11)"
      to: "g_current_range"
      via: "direct read of g_current_range.high_price/low_price/is_valid"
      pattern: "g_current_range\\.(is_valid|high_price|low_price)"
    - from: "render_dashboard()"
      to: "current price-mid (close or hl2)"
      via: "local computation for Range %"
      pattern: "compute_range_percent"
---

<objective>
Add PD Zone and Range % rows to the dashboard so the trader sees current zone state and range position at a glance.

Purpose: Completes the dashboard half of ROADMAP success criterion #4 (dashboard shows current PD zone name with color coding and current range percentage 0-100%). Pairs with Plan 02's chart rendering to give on-chart + dashboard visibility.

Output: Compiling Pine Script with 2 new dashboard rows following existing layout conventions (label in col 0, value in col 1, size.small, left/right halign).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-pd-zone-detection-grading-integration/02-CONTEXT.md
@.planning/phases/02-pd-zone-detection-grading-integration/02-RESEARCH.md
@.planning/phases/02-pd-zone-detection-grading-integration/02-01-PLAN.md
@.planning/phases/02-pd-zone-detection-grading-integration/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@src/IFVG_Indicator.pine
</context>

<interfaces>
From Plan 01 (src/IFVG_Indicator.pine):
- `var DealingRange g_current_range` — fields `is_valid`, `high_price`, `low_price`.
- `compute_range_percent(float price_mid)` — returns int 0-100 or na.

Existing dashboard (Section 11, ~lines 2308-2387):
- `var table dashboard = ...` declared at line ~2312.
- `render_dashboard()` called from main loop when `barstate.islast`.
- Existing rows 0-9 use `table.cell(dashboard, <col>, <row>, ...)` with `size=size.small`, `text_halign=text.align_left` for col 0 and `text.align_right` for col 1.
- Existing color convention: grade colors green/red/yellow/gray already used for HTF Bias row (reference pattern).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PD Zone and Range % rows to render_dashboard()</name>
  <files>src/IFVG_Indicator.pine</files>
  <read_first>
    - CLAUDE.md (Pine v6 rules — no blank lines in `if`/`switch` bodies; single-line `and`/`or`; `int()` cast where needed)
    - .planning/phases/02-pd-zone-detection-grading-integration/02-CONTEXT.md (D-06 no-range state, D-11 PD Zone row colors, D-12 Range % integer-only no-arrows)
    - .planning/phases/02-pd-zone-detection-grading-integration/02-RESEARCH.md §Code Examples §5 (dashboard cell pattern), Open Question 1 (row placement — append as rows 10, 11)
    - .planning/phases/02-pd-zone-detection-grading-integration/02-01-PLAN.md (globals + `compute_range_percent` signature)
    - src/IFVG_Indicator.pine Section 11 `render_dashboard()` (identify existing row count and column width declarations, match style)
    - src/IFVG_Indicator.pine lines 2308-2387 (existing `table.cell` calls — match argument order and text size)
  </read_first>
  <action>
Make the following edits to `src/IFVG_Indicator.pine`:

1. **Section 11 `render_dashboard()` — if the table is declared with a fixed row count via `table.new(..., rows = N)`, increase `N` by 2.** Locate the `table.new(...)` call in Section 11 (near line 2312). If rows is hardcoded (e.g., `rows = 10`), change it to `rows = 12`. If the table is declared with a variable or is overgrown-safe, skip this step.

2. **Section 11 `render_dashboard()` — immediately before the final cell-render block (i.e., at the end of the function body, after the last existing `table.cell` call but before any return), insert the following PD Zone + Range % rendering code. Row indices 10 and 11 (append at bottom).**

```
    // ── PD Zone row (DSH-01, D-11) ─────────────────────────────
    string pd_text  = "—"
    color  pd_color = color.gray
    int    row_pd   = 10
    int    row_range = 11
    if not na(g_current_range) and g_current_range.is_valid
        // Derive zone from current price-mid (close) against range EQ
        float current_mid = close
        float hi = g_current_range.high_price
        float lo = g_current_range.low_price
        if hi > lo
            float eq = (hi + lo) / 2.0
            if current_mid > eq
                pd_text := "PREMIUM"
                pd_color := color.red
            else if current_mid < eq
                pd_text := "DISCOUNT"
                pd_color := color.green
            else
                pd_text := "EQ"
                pd_color := color.yellow
    table.cell(dashboard, 0, row_pd, "PD Zone:", text_color=color.gray, text_size=size.small, text_halign=text.align_left)
    table.cell(dashboard, 1, row_pd, pd_text, text_color=pd_color, text_size=size.small, text_halign=text.align_right)

    // ── Range % row (DSH-02, D-12) ─────────────────────────────
    string range_pct_text = "—"
    if not na(g_current_range) and g_current_range.is_valid
        int pct = compute_range_percent(close)
        if not na(pct)
            // Clamp to 0-100 defensively (price can poke beyond range boundaries)
            int pct_clamped = math.max(0, math.min(100, pct))
            range_pct_text := str.tostring(pct_clamped) + "%"
    table.cell(dashboard, 0, row_range, "Range %:", text_color=color.gray, text_size=size.small, text_halign=text.align_left)
    table.cell(dashboard, 1, row_range, range_pct_text, text_color=color.white, text_size=size.small, text_halign=text.align_right)
```

Notes on CLAUDE.md compliance:
- All `if` bodies contiguous (no blank lines).
- `math.max(0, math.min(100, pct))` returns int because both args are int — no cast needed here. (`compute_range_percent` already returns int, so `pct_clamped` is int.)
- Single-line `and`/`or`.
- No trailing `=`.

3. **Confirm the main-loop guard for dashboard rendering is unchanged** (existing code already calls `render_dashboard()` only on `barstate.islast`; this is correct — the PD rows inherit this guard). Do NOT duplicate the guard.

4. **Do NOT modify `score_pd_zone()`, `calculate_grade()`, or any other grading logic.** PDZ-03 is already wired via Phase 5; Plan 01's `compute_pd_zone` injection at line 1684 makes the grading effect observable. The +1/-1 effect is verified via IFVG tooltip inspection (VALIDATION.md row PDZ-03), no additional code in this plan.
  </action>
  <verify>
    <automated>grep -n "PD Zone:" src/IFVG_Indicator.pine && grep -n "Range %:" src/IFVG_Indicator.pine && grep -n "compute_range_percent(close)" src/IFVG_Indicator.pine && grep -n "pd_text := \"PREMIUM\"" src/IFVG_Indicator.pine && grep -n "pd_text := \"DISCOUNT\"" src/IFVG_Indicator.pine && grep -n "pd_text := \"EQ\"" src/IFVG_Indicator.pine</automated>
  </verify>
  <acceptance_criteria>
    - `src/IFVG_Indicator.pine` contains exactly one `"PD Zone:"` label cell and one `"Range %:"` label cell.
    - PD Zone text mappings exist: `PREMIUM` → `color.red`, `DISCOUNT` → `color.green`, `EQ` → `color.yellow`, fallback `—` → `color.gray`.
    - Range % uses `str.tostring(pct_clamped) + "%"` format (integer + `%`, no decimals, no arrows).
    - When `g_current_range` is na or `is_valid = false`, both cells render `—` (gray for PD Zone; white for Range %) — verified by reading the code path.
    - `table.new(...)` row count (if fixed) is sufficient for rows 0..11 (i.e., `rows >= 12`).
    - Manual compile gate: paste into TradingView Pine Editor → no compile errors.
    - Visual gate (VALIDATION.md rows DSH-01, DSH-02): dashboard shows PD Zone row with correct color and Range % row with 0-100% integer; young chart with no range shows `—` for both.
    - PDZ-03 visual check (from VALIDATION.md): with Plan 01 + Plan 03 loaded, two bullish IFVGs — one in discount, one in premium — show different grades (discount higher). Tooltip `pd_score` = 2 (discount-bullish) vs 0 (premium-bullish).
  </acceptance_criteria>
  <done>
    Dashboard shows `PD Zone:` and `Range %:` rows with correct colors, values, and fallback states. Grading effect (PDZ-03) is observable via label tooltip differences on optimal vs. wrong-zone setups.
  </done>
</task>

</tasks>

<verification>
- Compile gate (TradingView Pine Editor) passes.
- On NQ1! 15m with PD=D: PD Zone row shows PREMIUM (red) or DISCOUNT (green) based on current close vs. EQ; Range % shows a value 0-100 that cross-checks against `(close - low) / (high - low) * 100`.
- On a chart with no confirmed HTF pivots yet (e.g., very early on a low timeframe): both rows show `—`.
- Existing dashboard rows 0-9 still render correctly (no layout regression).
- Same-quality bullish IFVGs in discount vs. premium show grade differences driven by `pd_score`.
</verification>

<success_criteria>
- PD Zone row and Range % row appear on dashboard with correct colors and values.
- Fallback `—` state renders when no valid range.
- Maps to ROADMAP Phase 2 success criterion #4 (dashboard shows current PD zone name with color coding and range percentage) and success criterion #3 (grading differences observable — PDZ-03 visual test via tooltip).
</success_criteria>

<output>
After completion, create `.planning/phases/02-pd-zone-detection-grading-integration/02-03-SUMMARY.md` documenting:
- Row indices used (10 and 11 appended; note Phase 3 DSH-04 will re-layout).
- Color mapping decisions (semantic: red=PREMIUM=suboptimal-for-longs, green=DISCOUNT=optimal-for-longs, yellow=EQ, gray=no-range).
- Range % clamp rationale (price can briefly poke beyond ITH/ITL bounds before the sweep machinery invalidates the range).
- PDZ-03 observation: record observed grade distribution difference between discount and premium bullish IFVGs on the test chart.
- If `table.new` rows was modified, note the new row count.
</output>
